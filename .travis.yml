sudo: false
language: python
dist: bionic
python:
  - "3.6"
  - "3.7"
  - "3.8-dev"

env:
  global:
    - PIPENV_IGNORE_VIRTUALENVS=1

install:
  - pip install pipenv
  - pipenv install --dev

script:
  - flake8
  - python setup.py test

after_success:
  - coveralls
  - codecov
