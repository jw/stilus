def merge(*dest):
    # utils.assert_present(dest[0], 'dest')
    # dest = utils.unwrap(dest[0]).first()
    # utils.assert_type(dest, 'object', 'dest')

    # last = utils.unwrap(dest).first()
    # deep = (last.value is True)
    pass
