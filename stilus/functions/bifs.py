from stilus.functions import blue, math, math_prop, unit, length, addProperty

bifs = {
    '-math-prop': math_prop.math_prop,
    'blue': blue.blue,
    'math': math.math,
    'unit': unit.unit,
    'length': length.length,
    'addProperty': addProperty.addProperty,
}

raw_bifs = ['length', 'addProperty']
